# This recipe compiles opencv once (on python_min, against Qt6) and splits the
# result into three packages: libopencv, py-opencv and opencv.
#
# libopencv holds the native C++ libraries, headers, cmake/pkg-config files and
# the opencv_* command line tools. It contains no python and no Qt, and is
# shared by every python version and by both the headless and GUI use cases.
# opencv-dnn changes on most patch versions, so libopencv pins itself to the
# patch version x.x.x via run_exports.
#
# py-opencv holds the cv2 python bindings AND the Qt plugins. The bindings are
# compiled against CPython's stable ABI (abi3) using OpenCV's PYTHON3_LIMITED_API
# support, so a *single* build per hdf5 configuration is produced on python_min
# and then works on every later python -- instead of one build per python minor
# version. conda-forge drives this through the is_abi3 / is_python_min /
# python_min variant variables and the python-abi3 host dependency (which
# supplies the `cpython >={{ python_min }}` lower bound). The bindings must be
# split out of libopencv because a python_version_independent (noarch/abi3)
# package may not contain native executables such as opencv_*.
#
# The Qt6 backends -- the highgui window backend (opencv_highgui_qt, patches
# 0005/0006) and the cvv debug-visualization (opencv_cvv_qt, contrib patch 0002)
# -- are built as dynamically loaded plugins and shipped inside py-opencv. They
# are loaded on demand: py-opencv only *run_constrains* qt6-main (so qt6 is never
# forced), and GUI calls work whenever a compatible qt6 is present. This replaces
# the old qt_version build-matrix axis: one build serves both headless and GUI,
# because Qt is loaded at runtime rather than linked into libopencv.
#
# opencv is a backward compatible metapackage that depends on the exact
# libopencv and py-opencv builds plus qt6-main (so `conda install opencv` keeps
# imshow working); install py-opencv for a headless stack.
{% set version = "4.13.0" %}
{% set major_version = version.split('.')[0] %}

{% set build = 11 %}

package:
  name: libopencv
  version: {{ version }}

source:
  - url: https://github.com/opencv/opencv/archive/{{ version }}.tar.gz
    fn: opencv-{{ version }}.tar.gz
    sha256: 1d40ca017ea51c533cf9fd5cbde5b5fe7ae248291ddf2af99d4c17cf8e13017d
    patches:
      # backport https://github.com/opencv/opencv/pull/21611 (unmerged as of 06/2023)
      - patches_opencv/0001-Add-installation-of-pip-metadata-from-cmake.patch
      - patches_opencv/0001-Also-install-metadata-for-opencv-python-headless.patch
      - patches_opencv/0003-find-pkgconfig-on-windows.patch
      - patches_opencv/0004-fix-detection-for-protobuf-23.x.patch
      # https://github.com/opencv/opencv/pull/28345
      - patches_opencv/28345-fix-vsx-utils-syntax-error.patch
      # https://github.com/opencv/opencv/pull/28592
      - patches_opencv/28592-Fix-OpenVINO-2026.patch
      # Make the Qt highgui backend a dynamically loaded plugin so that a single
      # build (compiled WITH_QT=6) works with or without Qt present at runtime.
      # Adds a cv::highgui_backend::UIBackend implementation for Qt ...
      - patches_opencv/0005-highgui-qt-add-UIBackend-plugin.patch
      # ... and the CMake + registry wiring to build/load it as opencv_highgui_qt.
      - patches_opencv/0006-highgui-build-qt-as-plugin.patch
  - url: https://github.com/opencv/opencv_contrib/archive/{{ version }}.tar.gz
    fn: opencv_contrib-{{ version }}.tar.gz
    sha256: 1e0077a4fd2960a7d2f4c9e49d6ba7bb891cac2d1be36d7e8e47aa97a9d1039b
    folder: opencv_contrib
    patches:
      # Allow attempt to find HDF5 on cross-compile
      - patches_opencv_contrib/cmake_hdf5_xpile.patch
      # Split the cvv debug-visualization module like highgui: a Qt-free
      # opencv_cvv forwarder (kept in libopencv) that dynamically loads an
      # opencv_cvv_qt plugin (shipped in py-opencv). Keeps libopencv Qt-free
      # while restoring cvv feature parity when qt is present.
      - patches_opencv_contrib/0002-cvv-dynamic-qt-plugin.patch
  - fn: test.avi
    url: https://github.com/opencv/opencv_extra/raw/master/testdata/highgui/video/VID00003-20100701-2204.avi
    sha256: 78884f64b564a3b06dc6ee731ed33b60c6d8cd864cea07f21d94ba0f90c7b310

build:
  number: {{ build }}
  # The whole project is compiled once on python_min (skip on any other
  # python). libopencv keeps the native C++ libraries and CLI tools, while the
  # cv2 python bindings are split into the python_version_independent py-opencv
  # output below. The bindings are built against CPython's stable ABI (abi3) via
  # -DPYTHON3_LIMITED_API=ON (see build.sh/bld.bat), so the single cv2.abi3.so
  # works on every later python. is_abi3/is_python_min/python_min come from the
  # conda-forge global pinning.
  skip: true                                                                      # [is_abi3 and not is_python_min]
  string: h{{ PKG_HASH }}_{{ PKG_BUILDNUM }}
  run_exports:
    # https://abi-laboratory.pro/index.php?view=timeline&l=opencv
    # Things seem to change every patch versions, mostly the dnn module
    - {{ pin_subpackage('libopencv', max_pin='x.x.x') }}
  ignore_run_exports_from:
    # python and numpy are only needed to *build* the cv2 bindings; the C++
    # libopencv package must not gain a python/numpy run dependency from them.
    - python
    - numpy
    # qt6-main is needed to build the opencv_highgui_qt plugin, but the plugin
    # (not libopencv) is what links Qt. libopencv must stay Qt-free so it can be
    # used headless; the qt dependency rides with py-opencv as a run_constraint.
    - qt6-main
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - libprotobuf                            # [build_platform != target_platform]
    - qt6-main                               # [build_platform != target_platform and not ppc64le]
    # pkg-config is required to find ffpmeg
    - pkg-config
    - cmake
    - ninja
    - libgomp                        # [linux]
    # ICE when enabling this
    # - llvm-openmp                    # [osx]
    - {{ stdlib('c') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - libgl-devel                        # [linux and not ppc64le]
    - libegl-devel                       # [linux and not ppc64le]
    # python and numpy are only here to build the cv2 bindings; their run
    # exports are ignored above so libopencv stays a pure C++ package. The abi3
    # run-export (cpython >={{ python_min }}) is attached to py-opencv instead.
    - python
    - numpy
    - eigen
    # Specify lgpl version of ffmpeg so that there are
    # no questions about the license of the resulting opencv
    # binary
    - ffmpeg {{ ffmpeg }} lgpl_*
    - freetype
    - harfbuzz
    - hdf5
    # Ensure that we build with nompi variant
    # it provides downstream packages with the most flexibility
    - hdf5 * nompi*
    - openjpeg
    - libavif
    - libcblas
    - libiconv                       # [unix]
    - libjpeg-turbo
    - liblapack
    - liblapacke
    - libpng
    - libprotobuf
    - libtiff
    - libwebp
    - openexr
    - libjxl
    # While imath doesn't trigger overlinking warnings
    # It does seem to be linked to opencv - libopencv_imgcodecs
    # https://github.com/conda-forge/opencv-feedstock/issues/486
    - imath
    # qt6-main is needed at build time to compile the opencv_highgui_qt plugin.
    # Its run-export is ignored above so libopencv itself stays Qt-free.
    # (ppc64le has no qt6, so it builds headless with no plugin.)
    - qt6-main                       # [not ppc64le]
    - zlib
    - libopenvino-dev                # [not ppc64le]
    # Why do I need expat here??? At the very least it helps find FFMPEG
    # And start the compilation on windows
    - expat

test:
  requires:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - pkg-config                      # [not win]
    # Test with the two currently supported lapack implementatons.
    # Only a single (python_min) build now happens, so split the backends
    # across platforms instead of across python versions.
    - liblapack * *openblas           # [not linux64]
    - liblapack * *mkl                # [linux64]
    - cmake
    - ninja
  files:
    - CMakeLists.txt
    - test.cpp
  commands:
    # Verify dynamic libraries on all systems
    {% set win_ver_lib = version|replace(".", "") %}
    # The bot doesn't support multiline jinja, so use
    # single line jinja.
    {% set opencv_libs = ["alphamat", "aruco", "bgsegm", "calib3d", "ccalib", "core", "datasets", "dnn", "dnn_objdetect", "dnn_superres", "dpm", "face", "features2d", "flann", "freetype", "fuzzy", "gapi", "hfs", "highgui", "img_hash", "imgcodecs", "imgproc", "intensity_transform", "line_descriptor", "mcc", "ml", "objdetect", "optflow", "phase_unwrapping", "photo", "plot", "quality", "rapid", "reg", "rgbd", "saliency", "shape", "stereo", "stitching", "structured_light", "superres", "surface_matching", "text", "tracking", "video", "videoio", "videostab", "wechat_qrcode", "xfeatures2d", "ximgproc", "xobjdetect", "xphoto"] %}
    - export MACOSX_DEPLOYMENT_TARGET={{ MACOSX_DEPLOYMENT_TARGET }}          # [osx]
    # hmaarrfk - 2024/10
    # Need to specify a default value for CONDA_BUILD_SYSROOT
    # otherwise the bot seems to fail to help us update
    # https://github.com/conda-forge/opencv-feedstock/issues/280
    {% if CONDA_BUILD_SYSROOT is defined %}
    - export CONDA_BUILD_SYSROOT={{ CONDA_BUILD_SYSROOT }}      # [osx]
    {% endif %}
    - OPENCV_FLAGS="`pkg-config --cflags opencv4`"                            # [unix]
    - $CXX -std=c++11 $RECIPE_DIR/test.cpp ${OPENCV_FLAGS} -o test            # [unix]
    - if [[ $(./test) != $PKG_VERSION ]]; then exit 1 ; fi                    # [unix]
    {% for each_opencv_lib in opencv_libs %}
    - echo Testing for presence of {{ each_opencv_lib }}
    - test -f $PREFIX/lib/libopencv_{{ each_opencv_lib }}${SHLIB_EXT}         # [unix]
    - if not exist %PREFIX%\\Library\\bin\\opencv_{{ each_opencv_lib }}{{ win_ver_lib }}.dll exit 1      # [win]
    - echo Found                   {{ each_opencv_lib }}
    {% endfor %}
    - test -f $PREFIX/lib/libopencv_bioinspired${SHLIB_EXT}      # [unix]
    - test -f $PREFIX/lib/libopencv_hdf${SHLIB_EXT}              # [unix]
    - mkdir -p cmake_build_test && pushd cmake_build_test
    - cmake -G "Ninja" ..
    - cmake --build . --config Release
    - popd

outputs:
  - name: libopencv
    # libopencv carries the native C++ libraries, headers, cmake/pkgconfig
    # files and the opencv_* CLI tools. Everything the build installed EXCEPT
    # the cv2 python bindings and the Qt plugins (both -> py-opencv). It is
    # Qt-free, so it can be used fully headless. A noarch/abi3 package may not
    # contain the opencv_* binaries, which is why the python bindings are split
    # out.
    files:
      include:
        - '**'
      exclude:
        - lib/python{{ python_min }}/site-packages/cv2                                      # [unix]
        - lib/python{{ python_min }}/site-packages/opencv_python-{{ version }}.dist-info             # [unix]
        - lib/python{{ python_min }}/site-packages/opencv_python_headless-{{ version }}.dist-info    # [unix]
        - lib/libopencv_highgui_qt*                                  # [unix]
        - lib/libopencv_cvv_qt*                                      # [unix]
        - Lib/site-packages/cv2                                      # [win]
        - Lib/site-packages/opencv_python-{{ version }}.dist-info             # [win]
        - Lib/site-packages/opencv_python_headless-{{ version }}.dist-info    # [win]
        - Library/bin/opencv_highgui_qt*                             # [win]
        - Library/bin/opencv_cvv_qt*                                 # [win]
  - name: opencv
    build:
      python_version_independent: true                                            # [is_abi3]
      string: h{{ PKG_HASH }}_{{ PKG_BUILDNUM }}
    requirements:
      run:
        - {{ pin_subpackage('libopencv', exact=True) }}
        - {{ pin_subpackage('py-opencv', exact=True) }}
        # opencv is the batteries-included metapackage. py-opencv carries the Qt
        # GUI plugins (loaded on demand) but only run_constrains qt6; pulling
        # qt6-main here is what activates the GUI for `conda install opencv`,
        # matching the previous qt6 build. Install py-opencv for a headless stack.
        # (ppc64le has no qt6 -> headless there.)
        - qt6-main                                                  # [not ppc64le]
    test:
      commands:
        - echo "tested in other outputs"

  - name: py-opencv
    # py-opencv holds the cv2 stable-ABI (abi3) python bindings produced by the
    # single python_min build, plus the Qt highgui and cvv plugins. The plugins
    # link Qt but py-opencv does NOT hard-require it -- qt6-main is expressed as a
    # run_constraint below, so py-opencv installs fine headless (the plugins stay
    # dormant) yet GUI calls work whenever a compatible qt6 is present.
    # python_version_independent makes the one build installable on every later
    # python; python-abi3 supplies the `cpython >={{ python_min }}` lower bound
    # (and excludes free-threading).
    build:
      python_version_independent: true                                            # [is_abi3]
      string: h{{ PKG_HASH }}_{{ PKG_BUILDNUM }}
      # The bundled Qt plugins intentionally link Qt without listing qt6-main as
      # a run dependency (it is a run_constraint instead), so allow that
      # "overlinking" and drop qt6-main's run-export.
      error_overlinking: false
      ignore_run_exports_from:
        - qt6-main
      # Do not byte-compile the cv2 .py files at build time. As a
      # python_version_independent (noarch) package the .pyc are regenerated for
      # the target interpreter at install time anyway, and compiling them here
      # would run the host python -- which is impossible when cross-compiling
      # (e.g. osx-64 -> osx-arm64): "Bad CPU type in executable".
      skip_compile_pyc:
        - "*.py"
      run_exports:
        # don't pin the python version so hard.
        # Good compatibility has been found in the python package
        - {{ pin_subpackage('py-opencv') }}
    files:
      include:
        - lib/python{{ python_min }}/site-packages/cv2                                      # [unix]
        - lib/python{{ python_min }}/site-packages/opencv_python-{{ version }}.dist-info             # [unix]
        - lib/python{{ python_min }}/site-packages/opencv_python_headless-{{ version }}.dist-info    # [unix]
        # The Qt plugins, loaded on demand by libopencv's highgui/cvv at runtime.
        - lib/libopencv_highgui_qt*                                  # [unix and not ppc64le]
        - lib/libopencv_cvv_qt*                                      # [unix and not ppc64le]
        - Lib/site-packages/cv2                                      # [win]
        - Lib/site-packages/opencv_python-{{ version }}.dist-info             # [win]
        - Lib/site-packages/opencv_python_headless-{{ version }}.dist-info    # [win]
        - Library/bin/opencv_highgui_qt*                             # [win]
        - Library/bin/opencv_cvv_qt*                                 # [win]
    requirements:
      host:
        - python
        - numpy
        - python-abi3                        # [is_abi3]
        # Present so pin_compatible() can resolve the qt6 version for the
        # run_constraint; its run-export is ignored above.
        - qt6-main                           # [not ppc64le]
      run:
        - python >={{ python_min }}
        - numpy
        - {{ pin_subpackage('libopencv', exact=True) }}
      run_constrained:
        # Keep opencv and Qt in sync without forcing Qt to be installed: if
        # qt6-main is present it must be ABI-compatible with the bundled plugins.
        - {{ pin_compatible('qt6-main', max_pin='x.x') }}           # [not ppc64le]
    test:
      requires:
        # Test with the two currently supported lapack implementatons.
        # Only a single (python_min) build now happens, so split the backends
        # across platforms instead of across python versions.
        - liblapack * *openblas         # [not linux64]
        - liblapack * *mkl              # [linux64]
        - pip
        - abi3audit                     # [is_abi3]
      imports:
        - cv2
        - cv2.xfeatures2d
        - cv2.freetype
      files:
        - run_py_test.py
        - color_palette_alpha.png
        - test_1_c1.jpg
      source_files:
        - test.avi
      commands:
        # Confirm cv2 was built against the stable ABI and only uses symbols
        # available from python_min onwards.
        - abi3audit $SP_DIR/cv2/python-3/cv2.abi3.so -s -v --assume-minimum-abi3 {{ python_min }}   # [unix and is_abi3]
        - abi3audit %SP_DIR%\\cv2\\python-3\\cv2.pyd -s -v --assume-minimum-abi3 {{ python_min }}    # [win and is_abi3]
        - python run_py_test.py
        - if [[ $($PYTHON -c 'import cv2; print(cv2.__version__)') != $PKG_VERSION ]]; then exit 1; fi  # [unix]
        - python -c "import cv2; assert 'Unknown' not in cv2.videoio_registry.getBackendName(cv2.CAP_V4L)"  # [linux]
        - python -c "import cv2, re; assert re.search('Lapack:\s+YES', cv2.getBuildInformation())"
        - python -c "import cv2, re; assert re.search(r'JPEG 2000:\s+OpenJPEG', cv2.getBuildInformation()), cv2.getBuildInformation()"
        - pip check
        - pip list
        - test $(pip list | grep "opencv-python " | wc -l) -eq 1           # [unix]
        - test $(pip list | grep "opencv-python-headless " | wc -l) -eq 1  # [unix]

about:
  home: https://opencv.org/
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Computer vision and machine learning software library.
  dev_url: https://github.com/opencv/opencv
  doc_url: https://docs.opencv.org/{{ major_version }}.x/

extra:
  recipe-maintainers:
    - h-vetinari
    - xhochy
    - jakirkham
    - msarahan
    - patricksnape
    - zym1010
    - hajapy
    - ocefpaf
    - hmaarrfk
    - ilya-lavrenov
